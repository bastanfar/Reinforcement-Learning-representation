using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace prjWindyGrid
{
    /// <summary>
    /// enum ActionType:
    ///     The Actions which the actor can do, in order to reach to the goal:
    /// </summary>
    public enum ActionType
    {
        Up = 0,
        Down = 1,
        Left = 2,
        Right = 3
    }

}
